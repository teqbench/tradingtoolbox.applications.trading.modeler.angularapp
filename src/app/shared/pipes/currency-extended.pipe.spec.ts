import { CurrencyExtendedPipe } from './currency-extended.pipe';

describe('CurrencyExtendedPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyExtendedPipe();
    expect(pipe).toBeTruthy();
  });
});
