/**
 * STAGING evironment configuration. Should be almost exactly the same and production; used for final testing.
 */

export const environment = {
  name: "staging",
  production: false,
  serviceTradingToolboxTradingModelerApiBaseUrl: ""
};
